# Dependencies
node_modules/

# Build outputs
out/
dist/
dist-site/

# Logs
*.log

# Local database / runtime
*.db
*.sqlite
*.sqlite3
dev.pid

# Env files (NEVER commit secrets). Only electron-builder.env.example is committed.
.env
.env.*
!.env.*.example

# OS specific
.DS_Store

# Code-signing certificates (NEVER commit)
*.p12
*.pfx
*.key
*.cer
*.cert
*.pem

# License signing private key (NEVER commit). Generated by scripts/license-keygen.mjs
.fortis-license-private.pem
*.license-private.pem
*.pem.local

# electron-builder local env (contains owner/publisher). Copy from electron-builder.env.example
electron-builder.env

# ---- INTERNAL ONLY (not for public/open-source release) ----
# AI assistants, dev-tooling, strategy/planning/agent artifacts. Kept local only.
.gemini/
.taskmaster/
.agents/
.agent/
.zcode/
.claude/
.cursor/
.kiro/
.windsurf/
.aider*
.github/copilot*
CLAUDE.md
GEMINI.md
AGENTS.md
*.mdc
.env.example
docs/
.dbq.cjs
skills-lock.json
install-skills.sh
