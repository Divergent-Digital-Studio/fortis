# Copy to electron-builder.env (gitignored) OR set these in your CI environment / GitHub Variables.
# electron-builder auto-loads ./electron-builder.env at build time.
#
# REQUIRED to publish + ship signed installers:
FORTIS_REPO_OWNER=your-github-org
FORTIS_PUBLISHER_NAME=Your Legal Entity Name
